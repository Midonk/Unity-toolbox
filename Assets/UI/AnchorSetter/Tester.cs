using UnityEngine;

[ExecuteInEditMode]
public class Tester : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //_rect.AnchorToCorner();
    }

    public RectTransform _rect;
}
